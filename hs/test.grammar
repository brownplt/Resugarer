core
  start CExpr
  values
    CNum : Int -> CExpr;
  constructors
    Plus : CExpr * CExpr -> CExpr;
surface
  start CExpr
  values
    SNum : Int -> CExpr;
  constructors
    Inc : CExpr -> CExpr;
rules
  Inc(n) -> Plus(n, CNum(1));
  SNum(n) -> CNum(n);
